## 1. Connectivity Check

- [x] 1.1 Open `src/components/Layout.tsx` and verify the theme toggle button has `onClick={toggleTheme}` correctly bound.
- [x] 1.2 Verify the button renders `{theme === 'dark' ? <Sun size={20} /> : <Moon size={20} />}` natively.
- [x] 1.3 Open `tailwind.config.ts` and ensure `darkMode: 'class'` is explicitly defined at the root object level.
