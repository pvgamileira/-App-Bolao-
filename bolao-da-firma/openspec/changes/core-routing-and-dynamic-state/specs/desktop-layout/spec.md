## MODIFIED Requirements

### Requirement: Global Layout Wrapper
The `App.tsx` (or a dedicated `Layout.tsx` file) MUST implement a global wrapper component.
- The wrapper MUST include a top navigation bar.
- The navigation bar MUST contain the Theme Toggle and links to Dashboard / Leaderboard.
- The navigation bar MUST ONLY appear if the user is authenticated.
